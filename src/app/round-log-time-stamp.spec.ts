import { RoundLogTimeStamp } from './round-log-time-stamp';

describe('RoundLogTimeStamp', () => {
  it('should create an instance', () => {
    expect(new RoundLogTimeStamp()).toBeTruthy();
  });
});
